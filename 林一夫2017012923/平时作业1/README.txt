1.用类封装了Factory，Factory可用于生成需要的随机数类型，可以使用自定义数据的类型，数据的量，数据的范围，如果随机数类型是字符型，还可以定义生成随机字符串的长度。
2.用类封装了dataScreening，dataScreening可用于筛选需要的随机数，可以自定义筛选数据的范围，必须预先指定需要筛选随机数的类型。
3.以上两种类，都自定义了一些可能出现的运行错误，如果运行出现了问题，可提供相关反馈。